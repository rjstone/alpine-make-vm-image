# Example cloud-init user-data configs

These are just some different cloud-init user-data configs that show how to
configure the os and install additional packages on first boot using this
image.

Because cloud-init is in use, the base image can remain minimal and you can
use it for just about anything without having to build special purpose images
for each configuration.
